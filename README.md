# machine-setup
Ansible scripts for setting up my development machine in different environments

# Running script
Running the entire script
```bash
ansible-playbook local.yml
```

Running a specific tag
```bash
ansible-playbook local.yml --tags window-manager
```

